const dottpath = {
  diffs: require("./diffs"),
  extract: require("./extract"),
  map: require("./map"),
  // queryObjects: require("./queryObjects"),
  // set: require("./set"),
};

module.exports = dottpath;
